<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EDU_HUNT" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_main</item>
        <item name="colorPrimaryVariant">@color/color_main</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Widget.MyApp.HeaderBar.Spinner" parent="Widget.AppCompat.Light.Spinner.DropDown.ActionBar">
        <item name="background">@color/white</item>
        <item name="android:popupBackground">@color/white</item>
        <item name="android:dropDownSelector">@color/black</item>
        <item name="android:divider">@color/black</item>
    </style>

    <style name="Progressbar.White" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorControlActivated">@color/color_main</item>
    </style>
    <style name="MyDarkToolbarStyle" parent="@style/Widget.AppCompat.Spinner.DropDown.ActionBar">
        <item name="background">@color/white</item>
        <item name="android:popupBackground">@color/white</item>
        <item name="android:dropDownSelector">@color/black</item>
        <item name="android:divider">@color/black</item>
    </style>

</resources>